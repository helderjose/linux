Print network connections, routing tables, interface statistics, masquerade connections, and multicast memberships


Verifica se a porta está em uso, se não imprimir nada, não está em uso
sudo netstat -pnlt | grep ':8080'
sudo netstat -pnlt | grep ':3000'


man netstat
netstat --help




------------------------
Parar node js
matar node js

sudo netstat -pnlt | grep ':3000'
kill -9 <numero-processo>